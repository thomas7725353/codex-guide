{
  "name": "codex-guide-worker",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "cf:deploy": "wrangler deploy",
    "cf:dry-run": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "wrangler": "^4.100.0"
  }
}
